name: multifit
channels:
    - conda-forge
dependencies:
    - python==3.9.*
    - numpy<2.1.0
    - matplotlib
    - scipy
    - setuptools
    - typing-extensions
    - pytest
    - pip
    -   pip:
            - mpyez
